export function renderPrediction(t){const n=t.created.toDate(),i=t.expires.toDate();return`\n    <div class="prediction-box" data-id="${t.id}">\n      <h2>${t.title}</h2>\n      <p>${t.description}</p>\n      <div class="time-status">\n        <span>Created: ${n.toLocaleDateString()}</span><br>\n        <span>Expires: ${i.toLocaleDateString()}</span><br>\n        <span class="status-${t.status}">Status: ${t.status}</span>\n      </div>\n      <button class="copy-link" onclick="copyLink('${t.id}')">Copy Link</button>\n    </div>\n  `}export function copyLink(t){const n=`${window.location.origin}/prediction/${t}`;navigator.clipboard.writeText(n).then((()=>alert("Link copied to clipboard!"))).catch((t=>console.error("Failed to copy link: ",t)))}export function debounce(t,n){let i;return function(...o){clearTimeout(i),i=setTimeout((()=>{clearTimeout(i),t(...o)}),n)}}