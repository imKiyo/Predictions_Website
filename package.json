{
  "name": "predictions_website",
  "version": "1.0.0",
  "description": "A website for predictions using Firebase",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --open",
    "build": "webpack --mode production && mkdir -p build && rsync -av --exclude='build' ./ build/",
    "test": "echo 'Running tests...' && exit 0"
  },
  "author": "imKiyo",
  "license": "MIT",
  "dependencies": {
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "dotenv-webpack": "^7.0.2",
    "webpack": "^5.50.0",
    "webpack-cli": "^4.7.2",
    "webpack-dev-server": "^4.0.0",
    "copy-webpack-plugin": "^9.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
